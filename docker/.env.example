# Docker 环境变量配置
# 与 .env.development.local 保持一致

# 通用配置
DOCKER_DATA_DIR=${HOME}/docker_data/ai-monorepo-dev

# PostgreSQL 配置
POSTGRES_DB=ai_dev
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres

# Redis 配置
REDIS_DB=0
REDIS_PASSWORD=redis
REDIS_PORT=6379
