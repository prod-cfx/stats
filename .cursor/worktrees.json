{
  "setup-worktree": [
    "find \"$ROOT_WORKTREE_PATH\" -maxdepth 1 -type f \\( -name \"*.local\" -o -name \".*.local\" \\) -exec cp {} ./ \\;",
    "pnpm install",
    "./scripts/dx build all --dev --offline"
  ]
}
