#!/usr/bin/env node

/**
 * DX CLI 入口（内部实现位于 scripts/lib/cli）。
 */

import { logger } from './lib/logger.js'
import { DxCli } from './lib/cli/dx-cli.js'

const cli = new DxCli()
cli.run().catch(error => {
  logger.error('CLI启动失败')
  logger.error(error.message)
  process.exit(1)
})
